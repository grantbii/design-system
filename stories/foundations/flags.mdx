import { IconGallery, IconItem, Meta } from "@storybook/addon-docs/blocks";
import { Flags, LOCATION_FLAG_MAP } from "../../index";
import { Location } from "@grantbii/ui-core/location/enums";

<Meta title="Foundation/Flags" />

# Country Flags

You can find the full gallery on this website:

[https://catamphetamine.gitlab.io/country-flag-icons/3x2/](https://catamphetamine.gitlab.io/country-flag-icons/3x2/)

<IconGallery>
  {Object.values(Location)
    .filter((location) => location !== Location.UNKNOWN && location !== Location.OTHERS)
    .map((location) => {
      const Flag = LOCATION_FLAG_MAP[location];

      return (
        <IconItem key={location} name={location}>
          <Flag size={32} />
        </IconItem>
      );
    })}

</IconGallery>
