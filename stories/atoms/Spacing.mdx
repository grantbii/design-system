import { Meta } from "@storybook/addon-docs/blocks";
import styled from "styled-components";
import { Color, Spacing } from "../../index";

<Meta title="Atoms/Spacing" />

# Spacing

<Table>
  <Row>
    <b>value</b>
    <b>visual</b>
  </Row>
  {spacingScale.map((spacingValue) => (
    <Row key={spacingValue}>
      <Label>{spacingValue}</Label>
      <Bar $width={spacingValue} />
    </Row>
  ))}
</Table>

export const spacingScale = [
  Spacing.px4,
  Spacing.px8,
  Spacing.px12,
  Spacing.px16,
  Spacing.px20,
  Spacing.px24,
  Spacing.px32,
  Spacing.px36,
  Spacing.px40,
  Spacing.px48,
  Spacing.px56,
  Spacing.px64,
  Spacing.px88,
  Spacing.px100,
];

export const Table = styled.div`
  display: flex;
  flex-direction: column;
  gap: ${Spacing.px12};
`;

export const Row = styled.div`
  display: grid;
  grid-template-columns: 64px minmax(0, 1fr);
  align-items: center;
  column-gap: ${Spacing.px24};
`;

export const Label = styled.p`
  margin: 0px;
`;

export const Bar = styled.div`
  width: ${(props) => props.$width};
  height: 32px;
  background: ${Color.brand.grantbiiYellow};
`;
